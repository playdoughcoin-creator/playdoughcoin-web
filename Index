<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <meta name="description" content="PlayDoughCoin ($PLDY) - The official relaunch of the most fun, fair meme coin. Join our squishy community today!">
  <meta name="keywords" content="PlayDoughCoin, PLDY, crypto, meme coin, token, blockchain">
  <meta name="author" content="PlayDoughCoin Team">
  <meta property="og:title" content="PlayDoughCoin • $PLDY Relaunch">
  <meta property="og:description" content="Join the PlayDoughCoin relaunch - A fair, fun meme coin built by the community.">
  <meta property="og:image" content="logo.png">
  
  <title>PlayDoughCoin • $PLDY Relaunch</title>
  
  <!-- Favicon -->
  <link rel="icon" type="image/png" href="logo.png">
  
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Chewy&family=Comic+Neue:wght@700&display=swap" rel="stylesheet">
  
  <style>
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    body {
      font-family: 'Comic Neue', cursive;
      background: #4fc3f7;
      color: white;
      min-height: 100vh;
      overflow-x: hidden;
    }

    .background {
      position: fixed;
      inset: 0;
      /* Using your Welcome Family image as the main background */
      background: linear-gradient(rgba(79, 195, 247, 0.8), rgba(79, 195, 247, 0.8)), url('welcome.png') center/cover no-repeat;
      z-index: -2;
    }

    .container {
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      padding: 60px 20px;
      position: relative;
    }

    .card {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(25px);
      -webkit-backdrop-filter: blur(25px);
      border: 4px solid rgba(255, 215, 0, 0.6);
      border-radius: 40px;
      padding: 50px 30px;
      max-width: 900px;
      width: 100%;
      text-align: center;
      box-shadow: 0 25px 80px rgba(0,0,0,0.4);
      position: relative;
      overflow: hidden;
    }

    .banner-img {
      width: 100%;
      height: auto;
      border-radius: 20px;
      margin-bottom: 30px;
      border: 3px solid #ffeb3b;
      box-shadow: 0 10px 20px rgba(0,0,0,0.2);
    }

    .title {
      font-family: 'Chewy', cursive;
      font-size: clamp(3.5rem, 10vw, 7rem);
      line-height: 0.92;
      margin-bottom: 10px;
      display: flex;
      justify-content: center;
      flex-wrap: wrap;
      gap: 4px 10px;
    }

    .title span {
      display: inline-block;
      animation: wobble 5s infinite;
      text-shadow: 4px 4px 0px rgba(0,0,0,0.2);
    }

    @keyframes wobble {
      0%,100% { transform: rotate(-3deg); }
      50% { transform: rotate(3deg); }
    }

    .c-yellow { color: #ffeb3b; }
    .c-orange { color: #ff5722; }
    .c-pink { color: #e91e63; }
    .c-purple { color: #9c27b0; }
    .c-cyan { color: #00bcd4; }

    .subtitle {
      font-size: clamp(1.8rem, 5vw, 2.8rem);
      margin: 10px 0 30px;
      color: #fff176;
      text-shadow: 2px 2px 4px rgba(0,0,0,0.3);
    }

    .intro {
      font-size: 1.3rem;
      max-width: 750px;
      margin: 0 auto 40px;
      line-height: 1.6;
      color: #fffde7;
      background: rgba(0,0,0,0.1);
      padding: 20px;
      border-radius: 20px;
    }

    .characters {
      position: absolute;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      pointer-events: none;
      z-index: -1;
    }

    .char {
      position: absolute;
      width: 100px;
      height: 100px;
      background-size: contain;
      background-repeat: no-repeat;
      animation: float 6s ease-in-out infinite;
    }

    @keyframes float {
      0%, 100% { transform: translateY(0) rotate(0deg); }
      50% { transform: translateY(-20px) rotate(10deg); }
    }

    .char.left { bottom: 15%; left: -30px; }
    .char.right { top: 10%; right: -30px; animation-delay: 1s; }

    .section h2 {
      font-family: 'Chewy', cursive;
      font-size: clamp(2.5rem, 6vw, 4rem);
      margin: 40px 0 30px;
      color: #ffeb3b;
      text-shadow: 3px 3px 0 #d81b60;
    }

    .roadmap-steps {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
      justify-content: center;
    }

    .step {
      background: rgba(255,255,255,0.1);
      border: 2px solid #ffeb3b;
      border-radius: 20px;
      padding: 20px;
      width: 100%;
      max-width: 260px;
    }

    .btn {
      display: inline-block;
      padding: 15px 35px;
      margin: 10px;
      border-radius: 50px;
      font-size: 1.2rem;
      font-weight: bold;
      text-decoration: none;
      transition: 0.3s;
      border: 3px solid white;
      cursor: pointer;
    }

    .btn-yellow { background: #ffeb3b; color: #d81b60; }
    .btn-blue { background: #00d4ff; color: white; }
    .btn:hover { transform: scale(1.1); }

    .socials { margin-top: 40px; }
    .socials a { color: white; margin: 0 15px; text-decoration: none; font-weight: bold; }

    footer { margin-top: 40px; opacity: 0.7; font-size: 0.9rem; }

    @media (max-width: 768px) {
      .roadmap-steps { flex-direction: column; align-items: center; }
      .step { max-width: 100%; }
      .char { display: none; }
    }
  </style>
</head>
<body>
  <div class="background"></div>
  <div class="container">
    <div class="card">
      
      <!-- Floating Logo Characters -->
      <div class="characters">
        <div class="char left" style="background-image: url('logo.png');"></div>
        <div class="char right" style="background-image: url('logo.png');"></div>
      </div>

      <!-- Main Logo Image -->
      <img src="logo.png" alt="PlayDoughCoin Logo" style="width: 180px; margin-bottom: 20px;">

      <div class="title">
        <span class="c-yellow">$</span>
        <span class="c-orange">P</span>
        <span class="c-pink">L</span>
        <span class="c-purple">D</span>
        <span class="c-cyan">Y</span>
      </div>

      <h1 class="subtitle">PlayDoughCoin <span style="color: #ffeb3b;">Relaunch</span></h1>

      <!-- About Banner -->
      <img src="about.png" alt="About PlayDoughCoin" class="banner-img">

      <p class="intro">
        This is the <strong>official relaunch</strong> of PlayDoughCoin — we're coming back stronger! 🍪✨<br><br>
        Our mission is to <strong>build a real, squishy, fun community</strong> before dropping the real thing. 
        No bots, no insiders, just the dough squad ready to mould the future!
      </p>

      <div>
        <a href="#roadmap" class="btn btn-yellow">View Roadmap</a>
        <a href="https://t.me/PlayDoughCoin" target="_blank" class="btn btn-blue">Join Telegram</a>
      </div>

      <!-- Roadmap Section -->
      <div class="section" id="roadmap">
        <h2>Our Roadmap</h2>
        <img src="roadmap-banner.png" alt="PlayDough Roadmap" class="banner-img">
        <div class="roadmap-steps">
          <div class="step">
            <h3 style="color: #ffeb3b;">Phase 1</h3>
            <p>Community Building & Character Design</p>
          </div>
          <div class="step">
            <h3 style="color: #ffeb3b;">Phase 2</h3>
            <p>Official $PLDY Relaunch & Marketing</p>
          </div>
          <div class="step">
            <h3 style="color: #ffeb3b;">Phase 3</h3>
            <p>CEX Listings & Dough Merch</p>
          </div>
        </div>
      </div>

      <!-- Community Banner -->
      <img src="community.png" alt="Join the Community" class="banner-img" style="margin-top: 40px;">

      <div class="socials">
        <a href="https://x.com/playdoughcoin" target="_blank">🐦 Twitter/X</a>
        <a href="https://t.me/PlayDoughCoin" target="_blank">✈️ Telegram</a>
      </div>

      <footer>
        <p>&copy; 2026 PlayDoughCoin. Made with 💛 by the dough squad.</p>
      </footer>
    </div>
  </div>
</body>
</html>
